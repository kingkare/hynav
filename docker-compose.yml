services:
  hynav:
    build: .
    image: hynav:latest
    container_name: hynav
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: "3002"
      HOSTNAME: "0.0.0.0"
    ports:
      - "127.0.0.1:3002:3002"
    volumes:
      - ./data:/app/data
